#include<iostream>
#include<cstdio>
#include<vector>
long long int fibo(int n)
{ 
  vector<long long int> vi;
  if (n == 0) 
    return vi.push_back(0);
  else if (n == 1)
    return vi.push_back(1);
  else
    return vi.push_back(
}

int main(){

std::ios::sync_with_stdio(false);

for (size_t i = 0; i < 50 ; i++ ){
   printf("%lld \n",fibo(i));

}


}

